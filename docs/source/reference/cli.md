# CLI

Every command and option below is generated directly from the Click application, so it cannot
drift from the implementation.

```{eval-rst}
.. click:: ai_guidelines.cli:guidelines
   :prog: guidelines
   :nested: full
```
